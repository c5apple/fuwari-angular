/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { NewProjectComponent } from '../app/articles/new-project/new-project.component';

describe('Component: NewProject', () => {
  it('should create an instance', () => {
    let component = new NewProjectComponent();
    expect(component).toBeTruthy();
  });
});
